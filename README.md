# how to run this project

1. clone this repository and cd into it. 
2. create a virtual env. and activate it. 
3. run the command ```pip install -r requirements.txt``` in your terminal
4. finally run ```streamlit run app.py``` in your termimal